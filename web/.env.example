# API URL for backend server
API_URL=http://localhost:4001

# Allowed email for authentication (single-user mode)
# This is the only email that can log in to the application
NEXT_PUBLIC_ALLOWED_EMAIL=your-email@example.com

# Password for authentication (single-user mode)
# This password is required along with the email to log in
NEXT_PUBLIC_ALLOWED_PASSWORD=your-secure-password
